{site.name}

## Navigation

=> gemini://{site.url}/~{site.username} Home
=> gemini://{site.url}/~{site.username}/about.gmi About

## {site.posts_subdir}

{{ for post in posts }}
=> gemini://{site.url}/~{site.username}/posts/{post.filename}.gmi {post.title}
{{ endfor }}

{{ if has_topics }}
## {site.topics_subdir}
{{ for topic in topics }}
=> gemini://{site.url}/~{site.username}/{topic.filename}.gmi {topic.title}
{{ endfor }}
{{ endif }}
